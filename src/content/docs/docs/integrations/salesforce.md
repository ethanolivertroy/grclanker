---
title: Salesforce
description: Read-only Salesforce security inspector covering Health Check, session and password policy, MFA, permissions, sharing, connected apps, login forensics, audit trail, encryption, and certificates with FedRAMP, CMMC, SOC 2, CIS, PCI-DSS, STIG, IRAP, and ISMAP mappings.
---

The Salesforce inspector audits one org through the REST API (SOQL), the Tooling API, and the Metadata API `listMetadata` and `readMetadata` calls. Every tool is read-only: nothing is created, updated, or deleted in the org.

## What it inspects

- Security Health Check score and per-setting risks (`SecurityHealthCheck`, `SecurityHealthCheckRisks`)
- `SecuritySettings` metadata: session timeout, forced logout, IP locking, password policies, trusted IP ranges, clickjack and CSRF flags, MFA for direct UI logins
- `MyDomainSettings` metadata: login policy for `login.salesforce.com` and API logins
- `Profile` metadata for sensitive profiles (System Administrator plus every profile with Modify All Data, View All Data, Manage Users, Author Apex, Customize Application, Manage Profiles and Permission Sets, or Password Never Expires): `loginHours` and `loginIpRanges`, resolved from SOQL profile IDs through `listMetadata(Profile)` and read with `readMetadata(Profile)` in batches of 10 (at most 50 profiles)
- Users, profiles, permission sets, permission set assignments, and `TwoFactorMethodsInfo` enrollment. `Permissions*` fields on `Profile` and `PermissionSet` are probed through `describeSObject` first, so fields that do not exist in the org's edition (for example `PermissionsApiUserOnly`) are omitted instead of failing the query
- The caller's own permission flags from `UserPermissionAccess` (`Customize Application`, `View All Users`, and the other permissions the inspector depends on)
- `FieldPermissions` on fields whose names look sensitive, organization-wide defaults from `Organization`
- `TenantSecret` (Shield Platform Encryption) and the Tooling API `Certificate` object (expiration, key size, CA-signed, exportable private key)
- `ConnectedApplication`, `OauthToken`, `LoginHistory`, `SetupAuditTrail`, `EventLogFile`

Every SOQL statement selects only fields listed in the Object Reference for that object. `OauthToken` is read without `CreatedDate`, `ConnectedApplication` without `CreatedDate` and `LastModifiedDate`, and `TwoFactorMethodsInfo` without `Id`, because those fields are not documented on those objects and an `INVALID_FIELD` error would take down the whole dataset.

## Setup and authentication

1. Create a connected app with OAuth enabled and the `api` scope (add `refresh_token` if you plan to use a refresh token). For the JWT bearer flow, upload the certificate whose private key the inspector will sign with and pre-authorize the auditing user's profile or permission set.
2. Give the auditing user these permissions: `API Enabled`, `View Setup and Configuration`, `View Health Check`, `View All Users`, `Modify Metadata Through Metadata API Functions` (required by `listMetadata` and `readMetadata`), `Manage MFA in API` (required to query `TwoFactorMethodsInfo`), `Customize Application` (without it `OauthToken` returns only the caller's own tokens), and, when licensed, `View Event Log Files` and `Manage Encryption Keys`. `salesforce_check_access` reads the caller's flags from `UserPermissionAccess` and lists any that are false.
3. Export credentials for one of the supported flows.

JWT bearer flow (recommended):

```bash
export SF_CONSUMER_KEY=3MVG9...
export SF_USERNAME=auditor@example.com
export SF_PRIVATE_KEY_FILE=/secure/server.key
export SF_INSTANCE_URL=https://acme.my.salesforce.com
```

Username-password flow (legacy, must be enabled under Setup > OAuth and OpenID Connect Settings):

```bash
export SF_USERNAME=auditor@example.com
export SF_PASSWORD=...
export SF_SECURITY_TOKEN=...
export SF_CONSUMER_KEY=3MVG9...
export SF_CONSUMER_SECRET=...
```

Other inputs:

- `SF_CREDENTIALS_FILE`: JSON file with `grant_type` (`jwt-bearer`, `password`, or `authorization_code` with a `refresh_token`) plus the matching fields (`instance_url`, `login_url`, `username`, `password`, `security_token`, `consumer_key`, `consumer_secret`, `private_key_file`, `refresh_token`, `access_token`, `api_version`)
- `SF_ACCESS_TOKEN` with `SF_INSTANCE_URL`: reuse a token from `sf org display`
- `SF_LOGIN_URL`: explicit login host; otherwise `https://test.salesforce.com` is used when `SF_SANDBOX=true` or the instance URL is a sandbox host, and `https://login.salesforce.com` otherwise
- `SF_API_VERSION`: defaults to `64.0`

Precedence is explicit tool arguments, then environment variables, then the credentials file.

## Tools

| Tool | Purpose |
|---|---|
| `salesforce_check_access` | Probes the OAuth session, Organization, limits, Health Check, SecuritySettings, users, profiles, Profile metadata (`listMetadata`), permission sets, `TwoFactorMethodsInfo`, login history, audit trail, connected apps, `OauthToken`, event log files, and the caller's `UserPermissionAccess` flags; lists likely missing permissions, including any caller flag that is false |
| `salesforce_assess_platform_security` | Controls 1, 2, 3, 5, 18, 19, 20 |
| `salesforce_assess_identity_access` | Controls 4, 6, 7, 9, 10, 13 |
| `salesforce_assess_data_protection` | Controls 8, 12, 16, 17 |
| `salesforce_assess_monitoring_integrations` | Controls 11, 14, 15 |
| `salesforce_export_audit_bundle` | Runs the access check and all four assessments, then writes `core_data/` (projected and redacted snapshots: `SecuritySettings`, `MyDomainSettings`, and `Profile` metadata reduced to the leaves the verdicts read, URL fields reduced to scheme, host, and path; each dataset wrapper carries `status`, `seen`, `total`, and `truncated`, and a dataset that was forbidden, unavailable, errored, or never requested is written as a `{ collected: false, dataset, status, http_status, endpoint, error }` marker instead of an empty list), `analysis/` (`findings.json`, per-area results with an `inventories` map naming each dataset as `complete`, `partial (seen of total rows)`, or `unread (status: error)`, `summary.json`), `compliance/` (executive summary, unified matrix, eight framework reports), `QUICK_REFERENCE.md`, `_errors.log` when collection partially failed, and a zip named after the allocated output directory |

## Verdict semantics

Verdict rules that apply to every finding: unreadable, forbidden, or errored data yields `manual` with the cause and the Setup evidence to collect; empty inventories never pass unless emptiness is compliant (only control 13, and only when the user list itself is non-empty); partial or truncated inventories downgrade `pass` to `warn` with seen and total counts; items missing a date are reported separately and never counted as current.

Population sanity gate: every org has a System Administrator profile with Modify All Data and at least one active user on it. Controls 4, 7, 9, 10, and 13 therefore render `manual` when the visible profile list has no administrator-class profile, or when a non-empty user list shows zero active administrators, because that view can only come from a credential without `View All Users` or setup visibility. The `population_view_issue` field in the identity summary states the reason.

### Partial and unread inputs

Every finding is gated on the datasets it reads (the `Inputs` column below):

- Unread input: when the finding's primary dataset was forbidden (401/403 or `INSUFFICIENT_ACCESS`), unavailable (`INVALID_TYPE`, `INVALID_FIELD`), or errored, the finding is `manual`, its summary names the dataset and the observed status ("... could not be verified because the TwoFactorMethodsInfo query was forbidden (403 ...)"), and `manualEvidence` states what to collect. A finding that also reads a secondary dataset (the Health Check MFA setting for SF-04, `PermissionSetAssignment` for SF-09, `EventLogFile` for SF-15) stops at `warn` while that dataset is unread and says what was not checked.
- Truncated input: a SOQL result that stopped at `record_limit`, returned fewer rows than `totalSize`, omitted `done` or `totalSize`, promised more rows without a `nextRecordsUrl`, whose cursor stopped advancing, or whose `nextRecordsUrl` pointed at another origin than the instance URL is `truncated` with a known or unknown total; the dataset line then carries the reason (`User read: partial (3 of 4000 rows; nextRecordsUrl pointed at another origin; not followed)`). Every request resolves against the session's instance URL and is sent to that origin only: a server-supplied absolute URL elsewhere is never fetched with the token (`Salesforce request URL pointed at another origin than the instance URL; the request was not sent.`). The finding's summary then carries "Only N of M ... records were read, so the verdict is downgraded" and a `pass` becomes `warn`. A claim over the rows not seen is not made from a partial read: SF-04 does not count or name users without a method, SF-07 renders the API Only absence and the users-on-API-profiles count `null` and caps at `warn`, SF-13 caps at `warn`, and SF-14 renders its failure ratio and every zero count (`brute_force_sources`, `legacy_tls_logins`, `rows_without_login_time`) `null` and caps at `warn`. A `fail` observed on a visible row (a stale administrator, an expired certificate, a public organization-wide default) stands.
- Documented row caps: `TwoFactorMethodsInfo` and `OauthToken` stop at 2500 rows with `done=true`, so a result of 2500 or more rows is treated as truncated (`two_factor_methods_possibly_capped`, `oauth_tokens_possibly_capped`); the SF-04 summary states which signal was observed (the cap, a larger `totalSize`, or `done=false` or a stalled cursor).
- Named principals: SF-04 renders `users_without_registered_mfa_method` and `sample_users_without_mfa` as `null` with a `principals_withheld` note whenever `User` or `TwoFactorMethodsInfo` is partial, and stops at `warn`; the visible users without an enrollment row are not named because the unread rows could hold their enrollments.
- Dependent datasets: `Profile` metadata is only requested for the sensitive profiles found in the `Profile` list; when that list is unread the metadata dataset carries `not requested: the Profile list was not readable (...)` with the parent's status, and SF-05 and SF-06 render `manual` naming the parent.
- Evidence and summaries: counts and lists derived from a dataset that was not read render `null` rather than `0` or `[]` (for example `active_users`, `users_seen`, `elevated_assignments`, `oauth_tokens`, `health_check_risks`); each finding's evidence carries the `*_truncated`, `seen`, and `total` values of the datasets it read; and every area summary carries an `inventories` map naming each dataset as `complete (N rows)`, `partial (N of M rows)`, or `unread (<status>: <error>)`.

## Control coverage

| # | Control | Tool | Finding | Inputs | Status semantics |
|---|---|---|---|---|---|
| 1 | Health Check score | platform_security | SF-01 | `SecurityHealthCheck`, `SecurityHealthCheckRisks` | pass when score >= 90 with zero high risks; warn >= 70; fail below; manual when `SecurityHealthCheck` is unread or exposes no `Score`, or when the risk list is unread (the score is still stated; `high_risk_settings`, `medium_risk_settings`, `settings_evaluated`, and `risks_truncated` render `null`); a truncated risk list turns pass into warn and sets `risks_truncated` |
| 2 | Session timeout | platform_security | SF-02 | `SecuritySettings` (`readMetadata`) | pass when `sessionTimeout` <= 2 hours, `forceLogoutOnSessionTimeout` true, and `lockSessionsToIp` true; warn without IP lock; fail otherwise; manual when `SecuritySettings` is unread (`Modify Metadata Through Metadata API Functions`) or the flags are absent |
| 3 | Password policy | platform_security | SF-03 | `SecuritySettings` | pass when length >= 12, complexity includes upper, lower, numeric, expiration <= 90 days, history >= 12, lockout enabled; warn on one gap; fail on two or more; manual when `SecuritySettings` is unread or a policy field is absent |
| 4 | MFA enforcement | identity_access | SF-04 | `SecuritySettings`, `SecurityHealthCheckRisks`, `User`, `Profile`, `TwoFactorMethodsInfo` | fail when `enableMFADirectUILoginOptIn` is false or the Health Check MFA setting does not meet the standard; when MFA is required: manual when `TwoFactorMethodsInfo` is forbidden (`Manage MFA in API`), the population gate trips, or zero active standard users are visible; pass when both `User` and `TwoFactorMethodsInfo` were read to completion and every active standard user has a registered method (warn instead when either is partial or the requirement's second source, `SecuritySettings` or the Health Check risk list, is unread); when users without a method are visible, fail when they exceed a quarter of the active standard users and warn otherwise, but only from a complete read: on a partial read (a 2500-row cap, a larger `totalSize`, `done=false`, or a stalled cursor) the finding stops at warn, the count and names render `null` with `principals_withheld`, and the summary states the observed signal. Manual when neither `SecuritySettings` nor Health Check exposes the MFA setting |
| 5 | IP range restrictions | platform_security | SF-05 | `SecuritySettings`, `Profile`, `Profile` metadata (`listMetadata`, `readMetadata`) | pass when every sensitive profile defines `loginIpRanges`, every sensitive profile was resolved and read, and `enforceIpRangesEveryRequest` is true; warn when some sensitive profiles lack ranges, a profile could not be resolved, only N of M sensitive profiles were read, or the flag is off; fail when no read profile defines ranges and no org-wide trusted ranges exist; manual when `SecuritySettings` is unread, the `Profile` list is unread or shows no administrator-class profile, `readMetadata(Profile)` is forbidden, or no sensitive profile could be resolved (the per-profile evidence renders `null`) |
| 6 | Login hour restrictions | identity_access | SF-06 | `Profile`, `Profile` metadata | pass when every sensitive profile bounds `loginHours` on all seven days (each `{weekday}Start`/`{weekday}End` pair is independent; a day with no start or end, or a 0 to 1440 window, is unrestricted) and every sensitive profile was resolved and read; warn when a profile bounds only some days (the unbounded days are named), some profiles restrict nothing, a profile could not be resolved, or only N of M sensitive profiles were read; fail when no read profile bounds any day; manual when the `Profile` list is unread or permission-limited, `readMetadata(Profile)` is forbidden, or no sensitive profile could be resolved |
| 7 | API access controls | identity_access | SF-07 | `Profile`, `User` | pass when <= 25 percent of profiles grant API Enabled; warn up to 50 percent; fail above; manual when the population gate trips; the summary states when `PermissionsApiUserOnly` does not exist in the org. When `Profile` or `User` is truncated the ratio is over the visible rows only, the verdict is capped at warn, and `api_only_profiles` and `active_users_on_api_enabled_profiles` render `null` unless a row was seen |
| 8 | Field-level security | data_protection | SF-08 | `FieldPermissions` | pass when no sensitive field has more than 5 grants; warn otherwise; manual when `FieldPermissions` is unread or no field matches the sensitive-name patterns; a truncated read turns pass into warn |
| 9 | Permission set review | identity_access | SF-09 | `PermissionSet`, `PermissionSetAssignment`, `User`, `Profile` | pass when no permission set grants elevated permissions, or when elevated sets have zero active assignees; warn when assignees exist up to `max_admins`; fail above it; manual when `PermissionSet` is unread or returns zero rows (standard sets always exist), the population gate trips, or elevated sets exist while `PermissionSetAssignment` is unread (`elevated_assignments`, `distinct_assignees`, and `assignments_truncated` render `null`); a pass with no elevated set is capped at warn while assignments are unread; a truncated set or assignment list turns pass into warn |
| 10 | Profile permissions | identity_access | SF-10 | `User`, `Profile` | pass when active admins <= `max_admins`, none stale, none undated; warn when an admin has no `LastLoginDate`; fail when admins exceed the threshold or any is stale (`stale_admins` are observed on visible rows and stand); manual when the population gate trips; a truncated user or profile list turns pass into warn and carries `users_seen`, `users_total`, and `*_truncated` |
| 11 | Connected app OAuth | monitoring_integrations | SF-11 | `ConnectedApplication`, `OauthToken`, `UserPermissionAccess` | warn at best: scopes are not exposed by SOQL; fail when most apps allow self-authorization; manual when `ConnectedApplication` is unread, returns zero apps, or no app exposes `OptionsAllowAdminApprovedUsersOnly`. Token usage is a partial view (`oauth_tokens_partial_view`) when the caller's `PermissionsCustomizeApplication` is false or unknown; `oauth_tokens_possibly_capped` marks a 2500-row result and `oauth_tokens_truncated` a partial read; `oauth_tokens`, `tokens_by_app`, and the three `oauth_tokens_*` flags render `null` and the summary says token usage was not checked when `OauthToken` is unread; `connected_applications_truncated` names its own source |
| 12 | Sharing settings | data_protection | SF-12 | `Organization` | warn at best: custom object defaults are not read; fail when 3+ standard objects are public; manual when `Organization` is unread or exposes no `Default*Access` field |
| 13 | Guest user access | identity_access | SF-13 | `User`, `Profile` | pass when a complete user list with at least one visible active administrator contains zero active guest users; warn when the user list is truncated (guest coverage is partial, the active guests seen are listed); fail when active guests sit on profiles with API Enabled or elevated permissions; warn when guests exist without them; manual when the population gate trips |
| 14 | Login forensics | monitoring_integrations | SF-14 | `LoginHistory` | on a complete read: fail when any source has 10+ failures, failures exceed 25 percent, or a legacy TLS login exists; warn when failures exceed 10 percent, more than 5 countries appear, or rows lack `LoginTime`; pass otherwise. On a truncated read the verdict is capped at warn, `failure_ratio` renders `null`, and a zero count of brute-force sources, legacy TLS logins, or undated rows renders `null` ("unknown from the visible rows"). Manual when `LoginHistory` is unread or returns zero rows in the window |
| 15 | Setup change tracking | monitoring_integrations | SF-15 | `SetupAuditTrail`, `EventLogFile` | pass when the trail is complete, every row is dated, no high-risk security change appears, and `EventLogFile` was readable; warn when the trail is truncated, rows lack `CreatedDate`, high-risk changes need review, or `EventLogFile` is unread (`event_monitoring` then carries the status and error instead of a count); manual when `SetupAuditTrail` is unread or returns zero rows in the window. The `EventLogFile` count is stated bare (`exposed N EventLogFile rows in 7 days`) only from a complete read: a truncated read says `exposed N of M EventLogFile rows ... (the read was truncated ...)`, or that the read was truncated before any row was visible when it showed none, `event_log_files_last_7_days` renders `null` for a zero from a truncated read, and `summary.event_log_files` is `null` under a partial read with `event_log_status` reading `partial` (`event_log_files_seen` and `event_log_files_total` carry the sample) |
| 16 | Data encryption | data_protection | SF-16 | `TenantSecret` | manual not-applicable when `TenantSecret` is forbidden or unavailable (never a pass); manual when the query errored; fail when a complete read returns no tenant secret, or none of the visible secrets is `Active`; manual (not fail) when the `TenantSecret` read was truncated before any secret was visible, since the absence is a claim about the unread rows (the summary carries the partial view and `tenant_secrets` renders `null`); warn when an active secret has no `CreatedDate` or the oldest is over 365 days old; a truncated read turns pass into warn. `summary.tenant_secrets` is a bare count of the org's secrets and renders `null` under a partial read, `tenant_secret_status` reads `partial` there (never `ok` beside a partial inventory line), and `tenant_secrets_seen` and `tenant_secrets_total` carry the sample |
| 17 | Certificate management | data_protection | SF-17 | `Certificate` (Tooling API) | fail when expired or key < 2048; warn when expiring within 30 days, undated, `KeySize` absent (never assumed to be 2048), `OptionsIsCaSigned` absent, private key exportable, or awaiting a signed chain; self-signed versus CA-signed counts are reported; manual when `Certificate` is unread or returns zero rows; a truncated read turns pass into warn |
| 18 | My Domain enforcement | platform_security | SF-18 | `MyDomainSettings` | pass when `canOnlyLoginWithMyDomainUrl` and `doesApiLoginRequireOrgDomain` are true; warn when only the API flag is off; fail when no My Domain is configured or legacy login is still allowed; manual when `MyDomainSettings` is unread or `canOnlyLoginWithMyDomainUrl` is absent |
| 19 | Clickjack protection | platform_security | SF-19 | `SecuritySettings` | pass when all four `enableClickjack*` flags are true; warn when one is false; fail when two or more are false; manual when `SecuritySettings` is unread or a flag is absent |
| 20 | CSRF protection | platform_security | SF-20 | `SecuritySettings` | pass when `enableCSRFOnGet` and `enableCSRFOnPost` are true; fail when either is false; manual when `SecuritySettings` is unread or the flags are absent |

## Framework mappings

Every finding carries the spec mapping for its control across FedRAMP, CMMC 2.0, SOC 2, CIS Salesforce, PCI-DSS 4.0, DISA STIG, IRAP, and ISMAP, for example `FedRAMP AC-12`, `CMMC 2.0 L2 AC.L2-3.1.10`, `SOC 2 CC6.1`, `CIS Salesforce 2.1`, `PCI-DSS 4.0 8.2.8`, `DISA STIG SRG-APP-000295`, `IRAP ISM-1164`, `ISMAP 8.2.8` for session timeout.

## Live smoke test

```bash
npm --prefix cli run test:salesforce:live
```

The script skips with exit code 0 when no credentials are present; otherwise it runs `salesforce_check_access` and the platform security assessment.

## Limitations and manual controls

- Profile `loginHours` and `loginIpRanges` are read only for sensitive profiles (System Administrator plus profiles with elevated permissions), capped at 50 profiles; profiles that `listMetadata(Profile)` cannot map to a metadata `fullName` are listed as unresolved and keep controls 5 and 6 at `warn` or below.
- `TwoFactorMethodsInfo` returns at most 2500 rows with `done=true`; a result of 2500 or more rows is treated as possibly truncated (`two_factor_methods_possibly_capped`), and on that or any other partial read of `TwoFactorMethodsInfo` or `User` control 4 stops at `warn`, withholds the count and names of users without a method (`principals_withheld`), and states which truncation signal was observed.
- `OauthToken` returns only the caller's own tokens when the caller lacks `Customize Application`, and the API gives no truncation signal; the inspector reads the caller's flag from `UserPermissionAccess` and marks control 11 as a partial view when it is false or unknown.
- `OauthToken` also has the same documented 2500-row hard cap as `TwoFactorMethodsInfo` (500 rows per query, `queryMore` up to 2500, then no more records, with `done=true`); an exactly 2500-row result is reported as possibly truncated in the control 11 evidence (`oauth_tokens_possibly_capped`) and summary, so a capped token count is never presented as complete.
- Connected app OAuth scopes, IP relaxation, and per-app policies beyond `OptionsAllowAdminApprovedUsersOnly` and refresh token validity are not exposed by SOQL.
- Custom object organization-wide defaults and sharing rules are not read; only the standard object defaults on `Organization` are evaluated.
- `Certificate` is a Tooling API object; self-signed certificates are reported but not failed on their own, since Salesforce issues self-signed certificates for JWT connected apps and SAML signing by design. Certificates without a `KeySize` value are reported as unknown and never counted as compliant.
- `listMetadata` and `readMetadata` require `Modify Metadata Through Metadata API Functions` or `Modify All Data`; without it, controls 2, 3, 5, 6, 18, 19, and 20 render as manual.
- `SecuritySettings`, `MyDomainSettings`, and `Profile` metadata trees are projected to the leaves the verdicts read before they are stored in `core_data/` (dropped section names are listed under `_omittedSections`), and every exported URL field (`LoginHistory.LoginUrl`, `ConnectedApplication.StartUrl`, and any other `*Url`) keeps scheme, host, and path only, because Salesforce carries `?sid=` and `?key=` values there. SOQL results whose `nextRecordsUrl` stops advancing or that omit `done`/`totalSize` are reported as truncated with an unknown total, and a finding that also reads a secondary inventory (for example permission set assignments or `EventLogFile`) stops at `warn` or `manual` while that inventory is unreadable.
- Every string a snapshot, an evidence list, or a summary keeps from a Salesforce response passes the data-side pass at the collector (`collectRecords`, `collectRecord`, and `collectProfileMetadata`): the client's remembered credentials in every form and every carrier (`Authorization`, `Cookie`, `Set-Cookie`, and API key headers, quoted or bare; URL userinfo and query strings, which go whole; credential-named assignments and fields; auth schemes; JWTs; PEM blocks) and, bare, the vendor-prefixed token shapes (`sk_live_`, `xoxb-`, `ghp_`, `AKIA`, and the rest) are removed from a description, a note, a name, or a comment: JWTs, PEM blocks, and vendor-prefixed tokens are unambiguous credential shapes with no identifier collision, so they go on the data side too, while the generic bare-run rule does not run on data, so identifiers such as `prod-us-east-2026`, a UUID, an opaque 24-character id, or a quoted `Content-Type` header stay. A record nested deeper than 32 levels is replaced by `[REDACTED]` at that depth rather than passed through (the payload is server-controlled).
- Error strings never quote a response body. A body without Salesforce's documented `message`, `errorCode`, `error`, or `error_description` field (a proxy or gateway page, whatever its content type claims, or JSON without those fields) is described as "non-JSON body (text/html, 5120 bytes)" or "JSON body without a recognized error field (...)" on the REST, Tooling, and token endpoints, and as "non-SOAP body (...)" or "SOAP body without a fault string (...)" on the Metadata API and SOAP `login()` calls, where only `faultcode` and `faultstring` are quoted. Every error string is passed through one redaction pass when `SalesforceApiError` is constructed and again when the error is recorded, so `access_check.json`, dataset `error` fields, `analysis/*.json` `errors` arrays, `_errors.log`, and tool results carry status, path, and shape only. The pass removes the configured and obtained credentials whatever their shape and in their base64, base64url, URL-encoded, and JSON-escaped forms; any value inside a carrier whatever its shape (`Authorization`, `Cookie`, `Set-Cookie`, and API key headers, cookie and session assignments, URL userinfo and query pairs, the `Bearer`, `Basic`, `Digest`, `Token`, and `ApiKey` schemes, credential-named fields and assignments, SOAP `sessionId` and other credential elements, command-line flags); and bare values only when they have a real token shape (JWT-shaped assertions, PEM blocks, hex digests, vendor-prefixed keys, and runs of 16 or more characters with base64 symbols, scattered digits, or token casing, including a run standing after `=` under a non-credential pair name such as `theme=`). A carrier or token shape standing right after a literal JSON escape (`\n`, `\u000a`, `\/`, and the rest, as a doubly-encoded body carries them) is treated exactly as one after a real newline, tab, or space. A bare name-shaped value standing alone in prose, such as an object name or `prod-us-east-2026`, is indistinguishable from a resource name and stays.
- Credentials file errors are fixed text. A file that cannot be read fails with "Unable to read Salesforce credentials file <path> (<CODE>)" (the Node error code only, `ENOENT` included); a file that cannot be parsed fails with "Unable to parse Salesforce credentials file: invalid JSON in <path> at line N" (the line only when `JSON.parse` reports a position). Neither `JSON.parse`'s message, which quotes a window of the source, nor the filesystem's wording is ever included; the private key file takes the same read rule ("Unable to read Salesforce private key file <path> (<CODE>)").
- Shield Event Monitoring and Platform Encryption are add-on licenses; unavailable objects render as manual not-applicable, never pass.
- The username-password flow is disabled by default in newer orgs and is kept only for legacy compatibility.

## Official documentation

- [REST API: Execute a SOQL Query](https://developer.salesforce.com/docs/atlas.en-us.api_rest.meta/api_rest/dome_query.htm) (`nextRecordsUrl`, `done`, `totalSize`)
- [Tooling API: SecurityHealthCheckRisks](https://developer.salesforce.com/docs/atlas.en-us.api_tooling.meta/api_tooling/tooling_api_objects_securityhealthcheckrisks.htm) (includes the `SecurityHealthCheck` Score query) and [Tooling API: Certificate](https://developer.salesforce.com/docs/atlas.en-us.api_tooling.meta/api_tooling/tooling_api_objects_certificate.htm)
- [Metadata API: SecuritySettings](https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_securitysettings.htm)
- [Metadata API: MyDomainSettings](https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_mydomainsettings.htm)
- [Metadata API: readMetadata()](https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_readMetadata.htm) (limit of 10 fullNames per call), [listMetadata()](https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_listmetadata.htm), and [Profile](https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_profile.htm) (`loginHours`, `loginIpRanges`)
- [Object Reference: UserPermissionAccess](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_userpermissionaccess.htm) (the caller's `Permissions<Name>` flags)
- [Object Reference: LoginHistory](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_loginhistory.htm), [SetupAuditTrail](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_setupaudittrail.htm), [ConnectedApplication](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_connectedapplication.htm), [OauthToken](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_oauthtoken.htm), [TwoFactorMethodsInfo](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_twofactormethodsinfo.htm), [PermissionSet](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_permissionset.htm), [PermissionSetAssignment](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_permissionsetassignment.htm), [Organization](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_organization.htm), [TenantSecret](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_tenantsecret.htm), [FieldPermissions](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_fieldpermissions.htm), [EventLogFile](https://developer.salesforce.com/docs/atlas.en-us.object_reference.meta/object_reference/sforce_api_objects_eventlogfile.htm)
- [OAuth 2.0 JWT Bearer Flow](https://help.salesforce.com/s/articleView?id=xcloud.remoteaccess_oauth_jwt_flow.htm&type=5) and [OAuth 2.0 Username-Password Flow](https://help.salesforce.com/s/articleView?id=xcloud.remoteaccess_oauth_username_password_flow.htm&type=5)
